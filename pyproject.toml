[project]
name = "ar-models"
version = "0.1.0"
description = "Static AR model publishing pipeline for VTP and VTU files"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = [
  "src",
]
